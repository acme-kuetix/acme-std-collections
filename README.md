# acme-std-collections

acme-std-collections provides WSL-callable iteration primitives (Sum, Map, Filter, Append, RemoveWhere) so WSL workflows can express array operations without falling back to Go loops.
